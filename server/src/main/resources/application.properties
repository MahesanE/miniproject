spring.datasource.url=jdbc:${MYSQL_URL}
spring.datasource.username=${MYSQLUSER}
spring.datasource.password=${MYSQLPASSWORD}



spring.data.mongodb.uri=mongodb://${MONGOUSER}:${MONGOPASSWORD}@${MONGOHOST}:6899/audit?authSource=admin&authMechanism=SCRAM-SHA-256


stripe.secretKey=${STRIPE_SECRET_KEY}

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

stripe.successUrl=https://good-meeting-production.up.railway.app/payment-success?session_id={CHECKOUT_SESSION_ID}
stripe.cancelUrl=https://good-meeting-production.up.railway.app/cart


